[project]
name = "specdec"
version = "0.1.0"
description = "Speculative decoding from scratch, for learning. Qwen3 draft/target on Apple Silicon."
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.15.0",
    "datasets>=5.0.1",
    "safetensors>=0.8.0",
    "torch>=2.14.0",
    "transformers>=5.17.0",
]

[tool.uv]
package = false
